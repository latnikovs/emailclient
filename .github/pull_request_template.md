## Summary

<!-- What does this change do, and why? Link the issue it closes. -->

Closes #

## Changes

<!-- Bullet the notable changes. -->

-

## Checklist

- [ ] `gofmt -l .` is clean, `go vet ./...` and `go test -race ./...` pass
- [ ] Tests added/updated for the change
- [ ] Docs updated (`README.md` / package docs) if the public API changed
- [ ] Wire-format changes are backwards compatible, or the version bump reflects it
- [ ] No dependencies added — this module is intentionally stdlib-only
